# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - codeharmony

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://codeharmonyregistry.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_dc838a09a1f449f3afe09cbdd668492c }}
        password: ${{ secrets.AzureAppService_ContainerPassword_c5a3af38199a4b6ab8c9241be0a36e82 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: codeharmonyregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_dc838a09a1f449f3afe09cbdd668492c }}/pdapi:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'codeharmony'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_23eb012862e34a5c8028660976dc0b73 }}
        images: 'codeharmonyregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_dc838a09a1f449f3afe09cbdd668492c }}/pdapi:${{ github.sha }}'